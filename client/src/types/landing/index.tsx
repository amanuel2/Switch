type activeType = {
    space: boolean,
    games_button: boolean,
    menu_overlay: boolean
}

type userType = {
    displayName: string
    email: string
    photoURL: string
    uid: string
}

export type { activeType, userType }